# URL of the running backend server
# For local development on a physical device, replace with your machine's LAN IP (e.g. http://192.168.x.x:5000)
# For emulator/simulator, localhost works
EXPO_PUBLIC_API_URL=http://localhost:5000
